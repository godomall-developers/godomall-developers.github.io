# 소스 변경 비교 (725)

- 변경 파일 수: 1개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Controller/Admin/Order/OrderPsController.php` |

---

## 상세 Diff

### Controller/Admin/Order/OrderPsController.php

```diff
--- a/Controller/Admin/Order/OrderPsController.php
+++ b/Controller/Admin/Order/OrderPsController.php
@@ -1054,10 +1054,9 @@ class OrderPsController extends \Controller\Admin\Controller
                 $postValue = Request::post()->toArray();
 
                 try {
-                    //회원의 장바구니 로드
-                    $cart = new CartAdmin($postValue['memNo'], true);
                     $cartSaveInfo = array();
                     foreach($postValue['cartSno'] as $key => $cartIdx){
+                        $cart = new CartAdmin($postValue['memNo'], true);
                         $cartInfo = array();
                         $cartInfo = $cart->getCartGoodsData($cartIdx);
                         if(count($cartInfo) > 0){
@@ -1067,11 +1066,11 @@ class OrderPsController extends \Controller\Admin\Controller
                                 }
                             }
                         }
+                        $cart = null;
+                        unset($cart);
                     }
 
                     //order write cart 에 저장
-                    $cart = null;
-                    unset($cart);
                     if(count($cartSaveInfo) > 0){
                         $cart = new CartAdmin($postValue['memNo']);
                         $resultCartSno = $cart->saveInfoMemberCartAdmin($cartSaveInfo);
```

