# 소스 변경 비교 (godo26)

- 변경 파일 수: 2개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Admin/goods/gift_present_register.php` |
| M | `Admin/goods/regular_goods_register.php` |

---

## 상세 Diff

### Admin/goods/gift_present_register.php

```diff
--- a/Admin/goods/gift_present_register.php
+++ b/Admin/goods/gift_present_register.php
@@ -67,6 +67,13 @@
                         return false;
                     }
 
+                    // 사은품 지급 수량 검증
+                    var giveCntVal = $.trim($('input[name="gift[giveCnt]['+i+']"]').val());
+                    if (parseInt(giveCntVal) > 10000) {
+                        $.validator.messages.optionCheck = '사은품 지급 수량은 최대 10,000개까지 입력하실 수 있습니다.';
+                        return false;
+                    }
+
                 }
                 return true;
             },'');
```

### Admin/goods/regular_goods_register.php

```diff
--- a/Admin/goods/regular_goods_register.php
+++ b/Admin/goods/regular_goods_register.php
@@ -428,8 +428,8 @@
             } else if (parseInt(giveCnt) < 1) {
                 alert("사은품 지급 수량은 0을 입력하실 수 없습니다.");
                 return false;
-            } else if (parseInt(giveCnt) > 127) {
-                alert("사은품 지급 수량은 127 이상을 입력하실 수 없습니다.");
+            } else if (parseInt(giveCnt) > 10000) {
+                alert("사은품 지급 수량은 최대 10,000개까지 입력하실 수 있습니다.");
                 return false;
             }
 
```

