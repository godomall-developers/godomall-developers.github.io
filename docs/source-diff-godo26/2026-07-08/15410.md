# 소스 변경 비교 (godo26)

- 변경 파일 수: 1개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Component/Bankda/BankdaGhostDepositor.php` |

---

## 상세 Diff

### Component/Bankda/BankdaGhostDepositor.php

```diff
--- a/Component/Bankda/BankdaGhostDepositor.php
+++ b/Component/Bankda/BankdaGhostDepositor.php
@@ -358,29 +358,12 @@ class BankdaGhostDepositor
             throw new AlertCloseException('사용할 수 없습니다.');
         }
 
-        $eval_json = array();
+        $eval_json = json_decode($json, true);
 
-        $_quoted = false;
-        $_out = '$eval_json=';
-
-        for ($i=0,$max=strlen($json); $i<$max; $i++) {
-
-            if (! $_quoted) {
-                if ($json[$i] == '{')		$_out .= ' array(';
-                else if ($json[$i] == '}')	$_out .= ')';
-                else if ($json[$i] == ':')	$_out .= '=>';
-                else if ($json[$i] == '[')	$_out .= ' array(';
-                else if ($json[$i] == ']')	$_out .= ')';
-                else $_out .= $json[$i];
-            } else {
-                $_out .= $json[$i];
-            }
-
-            if ($json[$i] == '"') $_quoted = ! $_quoted;
+        if (is_array($eval_json) === false) {
+            $eval_json = [];
         }
 
-        @eval($_out.';');
-
         $paging = '';
         $loop = [];
         $_page = [];
```

