# 소스 변경 비교 (825)

- 변경 파일 수: 2개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Admin/order/refund_do_new.php` |
| M | `Component/Order/ReOrderCalculation.php` |

---

## 상세 Diff

### Admin/order/refund_do_new.php

```diff
--- a/Admin/order/refund_do_new.php
+++ b/Admin/order/refund_do_new.php
@@ -2466,9 +2466,12 @@
                 if ($(this).val() == '') {
                     $(this).val(0);
                 }
-                if ($.isNumeric($(this).val())) {
-                    refundPriceSum += parseInt($(this).val());
-                    lessEachRefund = lessRefundPrice + parseInt($(this).val());
+                // 비숫자(콤마/음수부호 등) 제거 후 합계 검증 - 서버 정규화(NUMERIC_ONLY_PATTERN)와 동일하게 숫자만 남긴다.
+                // (미제거 시 $.isNumeric 검증을 우회해 그대로 전송 → 환불 안분 무한루프 유발)
+                var refundPriceVal = String($(this).val()).replace(/[^0-9]/g, '');
+                if ($.isNumeric(refundPriceVal)) {
+                    refundPriceSum += parseInt(refundPriceVal);
+                    lessEachRefund = lessRefundPrice + parseInt(refundPriceVal);
                 } else {
                     $(this).focus();
                 }
```

### Component/Order/ReOrderCalculation.php

```diff
--- a/Component/Order/ReOrderCalculation.php
+++ b/Component/Order/ReOrderCalculation.php
@@ -44,6 +44,12 @@ class ReOrderCalculation
 {
     use CommonLogStorageTrait; // 공용 로그 저장
 
+    // 환불 방법 설정의 결제수단별 완료 환불 금액 입력 키 (콤마 등 비숫자 정규화 대상)
+    const REFUND_COMPLETE_PRICE_KEYS = ['completeCashPrice', 'completePgPrice', 'completeDepositPrice', 'completeMileagePrice'];
+    // 숫자만 남기기 위한 정규식 - 천단위 콤마/음수부호 등 비숫자 제거용
+    // (음수부호를 남기면 "3-47180" 중간 절단, "-347180" 음수 통과로 안분 무한루프가 재발할 수 있어 숫자만 허용)
+    const NUMERIC_ONLY_PATTERN = '/[^0-9]/';
+
     /**
      * @var null|object 디비 접속
      */
@@ -1891,6 +1897,14 @@ class ReOrderCalculation
         $goods = \App::load(\Component\Goods\Goods::class);
         $orderAdmin = App::load(\Component\Order\OrderAdmin::class);
 
+        // 환불 방법 설정 금액값 정규화 - 콤마(예: "347,180") 등 비숫자가 입력되면 PHP 형변환 시 콤마 앞에서 잘려
+        // 안분 합계 불일치로 refundDivisionCheck 무한루프가 발생하므로, 산술 처리 전 정수로 강제 정제한다.
+        foreach (self::REFUND_COMPLETE_PRICE_KEYS as $completePriceKey) {
+            if (isset($getData['info'][$completePriceKey])) {
+                $getData['info'][$completePriceKey] = (int)preg_replace(self::NUMERIC_ONLY_PATTERN, '', (string)$getData['info'][$completePriceKey]);
+            }
+        }
+
         // 환불수단에 맞지않는 금액값이 넘어오면 리턴처리
         if ($getData['info']['refundMethod'] == '현금환불') {
             if ($getData['info']['completePgPrice'] > 0 || $getData['info']['completeDepositPrice'] > 0 || $getData['info']['completeMileagePrice'] > 0) {
@@ -3474,6 +3488,7 @@ class ReOrderCalculation
             }
             $checkDivisionVal = 0;
             do {
+                $beforeDivisionVal = $tempDivisionVal;
                 foreach ($arrayData as $k => $v) {
                     if ($checkKey != '') {
                         if (isset($v['orderDeliverySno']) && $checkKey != '' && $checkKey != $v['orderDeliverySno']) {
@@ -3497,6 +3512,17 @@ class ReOrderCalculation
                         }
                     }
                 }
+                // 무한루프 방지: 1회 순회 동안 차액(tempDivisionVal)이 전혀 줄지 않으면 더 안분할 항목이 없는
+                // 비정상 상태이므로(입력 금액 오류 등) 루프를 종료해 PHP 실행시간 초과(타임아웃)를 방지한다.
+                if ($checkDivisionVal === 0 && $tempDivisionVal === $beforeDivisionVal) {
+                    \Logger::channel('order')->warning(__METHOD__ . '[' . __LINE__ . '], 안분 차액을 맞출 수 없어 루프 종료(무한루프 방지): ', [
+                        'strText' => $strText,
+                        'remainDivisionVal' => $tempDivisionVal,
+                        'divisionSum' => $divisionSum,
+                        'orgPrice' => $orgPrice,
+                    ]);
+                    break;
+                }
             } while ($checkDivisionVal == 0);
         }
 
@@ -3545,6 +3571,10 @@ class ReOrderCalculation
         $checkDivisionVal = 1;
 
         do {
+            // 무한루프 방지: 직전 순회와 비교해 배송비 안분(tax) 합계가 전혀 변하지 않으면
+            // 더 이상 재분배로 조건을 만족시킬 수 없는 비정상 상태이므로 루프를 종료한다.
+            $beforeDivisionSignature = $tempTaxSupplySum . STR_DIVISION . $tempTaxVatSum . STR_DIVISION . $tempTaxFreeSum;
+
             // free supply vat 순으로 체크하면서 오버되면 재분배
             if ($orgTotalData['realTaxFreePrice'] < $tempTaxFreeSum) { // 환불 업데이트 free 합계값이 오버인경우
                 // 오버값
@@ -3829,6 +3859,17 @@ class ReOrderCalculation
                 $checkDivisionVal = 0;
             }
 
+            // 무한루프 방지: 재분배 후에도 tax 합계가 직전 순회와 동일하면 더 이상 진행이 없는 상태이므로 종료
+            if ($checkDivisionVal === 1 && $beforeDivisionSignature === ($tempTaxSupplySum . STR_DIVISION . $tempTaxVatSum . STR_DIVISION . $tempTaxFreeSum)) {
+                \Logger::channel('order')->warning(__METHOD__ . '[' . __LINE__ . '], 배송비 tax 안분이 수렴하지 않아 루프 종료(무한루프 방지): ', [
+                    'orgTotalData' => $orgTotalData,
+                    'tempTaxSupplySum' => $tempTaxSupplySum,
+                    'tempTaxVatSum' => $tempTaxVatSum,
+                    'tempTaxFreeSum' => $tempTaxFreeSum,
+                ]);
+                break;
+            }
+
         } while ($checkDivisionVal == 1);
 
         return [$updateDeliveryData, $refundDeliveryData];
```

