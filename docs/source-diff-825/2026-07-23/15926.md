# 소스 변경 비교 (825)

- 변경 파일 수: 2개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Component/Order/OrderNew.php` |
| M | `Component/Order/Order.php` |

---

## 상세 Diff

### Component/Order/OrderNew.php

```diff
--- a/Component/Order/OrderNew.php
+++ b/Component/Order/OrderNew.php
@@ -1250,8 +1250,9 @@ class OrderNew
         $kafka = new ProducerUtils();
         // 주문카운트 대상이 있을 때만 발행 (없으면 null 메시지 방지)
         if (!empty($arrOrderGoodsSno['mq'])) {
+            \Logger::channel('kafka')->info('process sendMQ - payload :', ['topic' => $kafka::TOPIC_PRODUCT_ORDER_COUNT, 'orderNo' => $this->orderNo, 'orderGoodsNos' => $arrOrderGoodsSno['mq']]);
             $result = $kafka->send($kafka::TOPIC_PRODUCT_ORDER_COUNT, $kafka->makeData($arrOrderGoodsSno['mq'], 'p'), $kafka::MODE_RESULT_CALLLBACK, true);
-            \Logger::channel('kafka')->info('process sendMQ - return :', $result);
+            \Logger::channel('kafka')->info('process sendMQ - return :', array_merge(['orderNo' => $this->orderNo], is_array($result) ? $result : []));
         }
 
         unset($arrOrderGoodsSno['mq']);
@@ -5991,8 +5992,9 @@ class OrderNew
         $kafka = new ProducerUtils();
         // 주문카운트 대상이 있을 때만 발행 (없으면 null 메시지 방지)
         if (!empty($mqData)) {
+            \Logger::channel('kafka')->info('process sendMQ - payload :', ['topic' => $kafka::TOPIC_PRODUCT_ORDER_COUNT, 'orderNo' => $orderNo, 'orderGoodsNos' => $mqData]);
             $result = $kafka->send($kafka::TOPIC_PRODUCT_ORDER_COUNT, $kafka->makeData($mqData, 'p'), $kafka::MODE_RESULT_CALLLBACK, true);
-            \Logger::channel('kafka')->info('process sendMQ - return :', $result);
+            \Logger::channel('kafka')->info('process sendMQ - return :', array_merge(['orderNo' => $orderNo], is_array($result) ? $result : []));
         }
         unset($mqData);
 
```

### Component/Order/Order.php

```diff
--- a/Component/Order/Order.php
+++ b/Component/Order/Order.php
@@ -2068,8 +2068,9 @@ class Order
         $kafka = new ProducerUtils();
         // 주문카운트 대상이 있을 때만 발행 (없으면 null 메시지 방지)
         if (!empty($arrOrderGoodsSno['mq'])) {
+            \Logger::channel('kafka')->info('process sendMQ - payload :', ['topic' => $kafka::TOPIC_PRODUCT_ORDER_COUNT, 'orderNo' => $this->orderNo, 'orderGoodsNos' => $arrOrderGoodsSno['mq']]);
             $result = $kafka->send($kafka::TOPIC_PRODUCT_ORDER_COUNT, $kafka->makeData($arrOrderGoodsSno['mq'], 'p'), $kafka::MODE_RESULT_CALLLBACK, true);
-            \Logger::channel('kafka')->info('process sendMQ - return :', $result);
+            \Logger::channel('kafka')->info('process sendMQ - return :', array_merge(['orderNo' => $this->orderNo], is_array($result) ? $result : []));
         }
         unset($arrOrderGoodsSno['mq']);
 
@@ -6300,8 +6301,9 @@ class Order
         $kafka = new ProducerUtils();
         // 주문카운트 대상이 있을 때만 발행 (없으면 null 메시지 방지)
         if (!empty($mqData)) {
+            \Logger::channel('kafka')->info('process sendMQ - payload :', ['topic' => $kafka::TOPIC_PRODUCT_ORDER_COUNT, 'orderNo' => $orderNo, 'orderGoodsNos' => $mqData]);
             $result = $kafka->send($kafka::TOPIC_PRODUCT_ORDER_COUNT, $kafka->makeData($mqData, 'p'), $kafka::MODE_RESULT_CALLLBACK, true);
-            \Logger::channel('kafka')->info('process sendMQ - return :', $result);
+            \Logger::channel('kafka')->info('process sendMQ - return :', array_merge(['orderNo' => $orderNo], is_array($result) ? $result : []));
         }
         unset($mqData);
 
```

