# 소스 변경 비교 (godo25)

- 변경 파일 수: 1개

## 변경 파일 목록

| 상태 | 파일 경로 |
|------|----------|
| M | `Component/Sms/SmsAuto.php` |

---

## 상세 Diff

### Component/Sms/SmsAuto.php

```diff
--- a/Component/Sms/SmsAuto.php
+++ b/Component/Sms/SmsAuto.php
@@ -492,8 +492,6 @@ class SmsAuto implements \SplSubject
         } catch (\Exception $e) {
             $exceptionMessage = __METHOD__ . ', ' . $e->getFile() . '[' . $e->getLine() . '], ' . $e->getMessage();
             $logger->warning($exceptionMessage);
-            \Logger::channel('exception')->info($exceptionMessage, $e->getTrace());
-
             return false;
         }
         $logger->info(__METHOD__ . ' Send Sms Result', $result);
```

